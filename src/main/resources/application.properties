# Spring gloable config
spring.application.name=my-blog-demo
server.port=8090

# Spring datasource
spring.datasource.driverClassName=com.mysql.jdbc.Driver
spring.datasource.url=jdbc:mysql://112.74.34.141:3306/blog?useUnicode=true&characterEncoding=utf-8&transformedBitIsBoolean=true
spring.datasource.username=test
spring.datasource.password=a123456

# Connection pool
spring.datasource.type=com.alibaba.druid.pool.DruidDataSource
spring.datasource.maxActive=20
spring.datasource.initialSize=1
spring.datasource.maxWait=60000
spring.datasource.minIdle=1
spring.datasource.validationQuery=select 'x'
spring.datasource.poolPreparedStatements=1
spring.datasource.maxOpenPreparedStatements=20

# Mybatis
mybatis.type-aliases-package=com.cang.blog.entity
mybatis.mapperLocations=classpath:mapper/*.xml

# Log
logging.config=classpath:log4j2.xml
#logging.level.root=info

# Spring actuator
management.security.enabled=false

# JWT Token
jwt.header=blog-login-authorization
jwt.secret=theSecretKey
jwt.expiration=10